import React from 'react';
import styled from 'styled-components';

const ArrivalsContainer = styled.div``;
const NewArrivals = () => {
  return <ArrivalsContainer>new arrivals</ArrivalsContainer>;
};

export default NewArrivals;
